<% if flash[:error] %>
    <div class="notice"><%= flash[:error] %></div>
  <% end %>

<h1>Create a Worksession</h1>

<%= render 'form' %>

<%= link_to 'Create 2 Weeks of Worksessions', create_worksessions_path, class: 'btn btn-default '  %>
<%= link_to 'Back', user_worksessions_path(user_id: current_user.id), class: 'btn btn-default '  %>
